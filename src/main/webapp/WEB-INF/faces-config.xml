<?xml version="1.0" encoding="UTF-8"?>
<faces-config xmlns="https://jakarta.ee/xml/ns/jakartaee"
              xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
              xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
                                  https://jakarta.ee/xml/ns/jakartaee/web-facesconfig_4_1.xsd"
              version="4.1">

    <application>

        <!-- Idiomas soportados por la aplicación (por defecto: español) -->
        <locale-config>
            <default-locale>es</default-locale>
            <supported-locale>es</supported-locale>
            <supported-locale>en</supported-locale>
            <supported-locale>pt</supported-locale>
            <supported-locale>fr</supported-locale>
        </locale-config>

        <!-- Bundle de traducciones: resources/traducciones/CRUD.properties, CRUD_ES, CRUD_EN, CRUD_PT, CRUD_FR -->
        <resource-bundle>
            <base-name>traducciones.CRUD</base-name>
            <var>msg</var>
        </resource-bundle>

    </application>

</faces-config>
